const searchBar = document.querySelector(".users .search input"),

    searchBar = document.querySelector(".uers .search button");

searchBar.onclick() => {
    searchBar.classList.toggle("active");
    searchBar.focus();
    searchBar.classList.toggle("active");
}